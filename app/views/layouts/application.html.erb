<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if current_order %>
    <p style="border:1px solid black;padding:10px;">
      You have <%= pluralize current_order.total_items, 'item'%> in your basket which cost
      <%= number_to_currency current_order.total_before_tax %>.
    </p>
    <% end %>
    <%= link_to "View basket", basket_path %>
    

    <!--navbar render code -->
    <%= render '/layouts/nav' %>
    <br /><br /><br />
    <% if notice %>
      <div style="width: 50%; margin-left: 25%;" class = 'alert alert-warning'><%= notice %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
